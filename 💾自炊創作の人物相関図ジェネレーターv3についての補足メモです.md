Opus4.6:

---
**まだ気になる点**

**1. importNames が pair系フィールドを持たない** v3の「変更しないもの」欄に「pairId, pairRole, deceasedは `""`, `""`, `false` で初期化」と書いてあるけど、実際のimportNamesの擬似コードには反映されてない。現行コードのimportNamesをそのまま使うと `pairId`/`pairRole`/`deceased` が `undefined` になる。指示書に擬似コード追記するか、実装者への注意書きがあると安全。

**2. addCharacter も同様** v3の「新規キャラ追加時のデフォルト値」セクションにpair系フィールドが書いてあるのでOKだけど、現行コードの `addCharacter` 本体の擬似コードは載ってない。実装者が見落とす可能性は低いが一応。

**3. copySingle の呼び出し側変更箇所が1箇所だけ示されている** 非編集モードの `onClick` だけ書き換え例があるけど、出力プレビュー下部の `<pre>` 内は行クリックでのコピーがない（現行もない）ので問題なし。ただ全コピーボタンの `copyAll` も outputMode 分岐が必要で、これは指示書の「コピー機能」セクションに文章では書いてあるが擬似コードがない。実装者が自力で書ける範囲ではある。

**4. 検索クエリの pairId 対応** 「検索機能は pairId でもヒットするように拡張してもよい（任意）」とあるが、任意のまま残すとペアID形式で作業中にpairIdで検索できなくて不便になりそう。実装コスト低いので「推奨」に格上げしてもいいかも。

**5. pairErrors の警告UI** 「出力プレビューの上に警告を表示」とあるが、具体的なJSX/スタイルの擬似コードがない。他のUI要素（‡バッジ、編集フォーム等）には詳細なスタイル指定があるのでここだけ抜けてる感じ。軽微だけど実装者の裁量幅が広がる箇所。

**6. 出力プレビューの切り替えボタンのスタイル** トグルボタンの見た目（アクティブ/非アクティブの色分け等）が未定義。既存の `C` 定数で賄えるけど、どの色を使うかは指定がない。

---

**結論：v3はほぼ実装可能な完成度**。上の1〜2が実装漏れリスクとしてやや高め、3〜6は実装者の判断で吸収できる範囲。引き継ぎ時に「importNames と addCharacter にもpair系3フィールド忘れずに」と一言添えれば十分かと。⏎

---

- 1,2 → **【実装時に必ず対応】** importNames・addCharacterにpair系3フィールド追加
- 3 → **【実装時に必ず対応】** copyAllにもoutputMode分岐を入れる
- 4 → **【推奨】** 検索をpairIdにも対応させる
- 5,6 → **【任意・裁量OK】** 警告UIとトグルボタンのスタイルは既存トーンに合わせてよし

---


**v3で解決済み（GPTからの指摘事項→v3指示へ反映確認OK）**

- ✅ block2は全キャラ出力と明記
- ✅ duplicateCharacterでpair系リセット
- ✅ deleteSelectedに孤立参照クリア追加
- ✅ updateCharにpair連動ロジック追加
- ✅ gender="N"時はpairRole自動セットしない旨明記
- ✅ "00"禁止・onBlur正規化ヘルパー追加
- ✅ 統計は‡のみカウント、単独未定も1組と数える
- ✅ pairList生成でfind方式（上書きではなく先頭採用）
- ✅ copySingleのシグネチャ変更（text→char）
- ✅ エッジケース3件を「許容・将来対応」として明記